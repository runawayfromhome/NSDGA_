NSDGA SECURITY ASSESSMENT DOCUMENTATION
Date: 2026-04-02
Scope: Read-only static code/config scan (no code changes made)
Overall Security Score: 33/100

==================================================
1) SCORING SUMMARY
==================================================
The system has useful security controls (rate limiting, hashing, encrypted casts),
but several high-impact authentication/authorization and secret-management risks
significantly lower the score.

Why 33:
- Critical auth and access-control weaknesses can lead to privilege escalation
  and possible authentication bypass.
- Sensitive secrets are present in environment configuration.
- Hardcoded default credentials are present in seeded accounts.

==================================================
2) PRIORITIZED FINDINGS
==================================================

[CRITICAL]
1. OTP verification uses loose comparison
   - File: app/Http/Controllers/LoginController.php:95
   - Code pattern: $user->otp_code == $request->otp
   - Risk: Loose comparison in PHP can enable edge-case bypass patterns.

2. Admin routes are protected by auth only (missing strict admin authorization)
   - File: routes/web.php:21
   - Related routes:
     - routes/web.php:23  (/admin/dashboard)
     - routes/web.php:33  (/admin/store)
     - routes/web.php:34  (/admin/delete/{id})
     - routes/web.php:47  (/accounts/{id}/toggle-lock)
   - Risk: Authenticated non-admin users may reach admin-level operations.

3. Secrets exposed in environment file
   - File: .env
   - Lines observed:
     - .env:3  (APP_KEY)
     - .env:4  (APP_DEBUG=true)
     - .env:53 (MAIL_USERNAME)
     - .env:54 (MAIL_PASSWORD)
   - Risk: If leaked/committed/shared improperly, credentials and app cryptographic
     context can be abused.

4. Hardcoded default account passwords in seeder
   - File: database/seeders/DefaultAccountsSeeder.php
   - Lines observed:
     - 23 (adminpass)
     - 35 (registrarpass)
     - 47 (studentpass)
   - Risk: Predictable credentials can lead to immediate account compromise.

[HIGH]
5. OTP values stored in plaintext in database fields
   - File: app/Http/Controllers/LoginController.php
   - Lines observed: 36, 157
   - Risk: Database exposure reveals active OTP values.

6. Account creation path lacks robust validation policy before persistence
   - File: app/Http/Controllers/LoginController.php
   - Lines observed: 126, 131, 132
   - Risk: Weak password policy / unsafe role assignment possibilities.

==================================================
3) POSITIVE CONTROLS IDENTIFIED
==================================================
1. Throttling is configured on sensitive flows
   - routes/web.php:9   (login throttle)
   - routes/web.php:13-15 (OTP verify throttle)
   - routes/web.php:17-19 (OTP resend throttle)

2. Password hashing is used
   - app/Http/Controllers/LoginController.php:131
   - database/seeders/DefaultAccountsSeeder.php:23,35,47

3. Encrypted model casts for selected PII fields
   - app/Models/User.php:28 (full_name encrypted)
   - app/Models/User.php:29 (phone_number encrypted)

4. Session handling on logout follows good baseline pattern
   - app/Http/Controllers/LoginController.php:114-116

==================================================
4) METHODOLOGY NOTES
==================================================
- Type: Static review only.
- No runtime penetration testing executed.
- No code was modified.
- Findings are based on current visible source and configuration.

==================================================
5) LIMITATIONS
==================================================
- Could not verify deployment environment controls (firewall/WAF/HTTPS termination,
  secret storage, CI/CD masking, production .env handling) from source alone.
- Final real-world risk may be higher or lower depending on infrastructure setup.

==================================================
6) RECOMMENDED NEXT ACTIONS (DOCUMENTATION ONLY)
==================================================
1. Enforce strict role authorization on all admin and account-management endpoints.
2. Replace loose OTP comparison with strict verification logic.
3. Rotate exposed secrets immediately and remove hardcoded credential patterns.
4. Disable debug in production and adopt secure secret management practices.
5. Strengthen account creation/update validation (password and role constraints).

End of report.
